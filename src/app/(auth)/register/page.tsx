import RegisterPageContent from "@/components/shared/RegisterPageContent";

export const metadata = { title: "Register — DocSlot" };

export default function RegisterPage() {
  return <RegisterPageContent />;
}